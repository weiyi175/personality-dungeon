# 人格迭代實驗（Personality Iteration Study）規劃 v1

> 起點：使用者假設「三週期中，第一週期＝9D 初始值，三次遺言對 9D 的加減累積保存」。
> 三項決策（2026-06-12）：① 保存內容＝**SBERT 向量先疊加再縮放**；② 比較結構＝**受試者間 A/B：迭代 vs 重置**；③ 起始接近度＝**每週期重新固定**。
> 本文把三項決策收斂成可實作的機制 + 可檢定的實驗設計。落地前請過 §9 的待定子決策。

---

## ★ STUDY FRAMING（pre-registered, binding — 2026-06-13；最高層級，凌駕後續所有節）

本 pilot 是 **narrative-led study**，不是純機制研究。code 已確認：人格迭代機制在玩家端**幾乎無 experiential footprint**——唯一隨臂別變化的視覺元素是過場 priming 文字行；雷達、接近度條、所有 HUD 兩臂相同，累積只暗藏在 RL 事件節奏（魯莽度由累積向量算）。因此：

1. **Primary claim 天花板**：本研究最多只能宣稱「**敘事連續性 priming** 改變了書寫一致性(E1)/主觀連續感(q4)」。**即使 E1/q4 顯著，明確不可宣稱「人格迭代機制有效 / 機制造成 felt continuity」**——因為機制在玩家端近乎隱形，效果幾乎全由 priming line 承載。此為 framing 層級的硬約束，非可選的 honesty caveat。

2. **「機制顯化 v2」＝ pilot 之後的獨立 follow-up study，現在不做**。其內容是讓 cumulative personality 在玩家端產生可見足跡（如雷達顯示累積、明示「過去人格仍在」的視覺），使機制本身具 experiential footprint，屆時才談得上檢定「機制」效應。**啟動條件**：本 pilot 的 E1/q4 確實能在人類雜訊下擠出可用訊號（否則先解決訊號問題）。

> 後續 §3 假設層級、§8d hierarchy、§8e audit 全部在此 framing 之下解讀。

---

## 0. 現況 gap（為什麼要新建）

現制三個週期**互相獨立、不累積**：
- [`start_next_cycle()`](src/ui/../../src/core/DungeonLifecycleController.gd) 進下一週期時 `_will_personality = []`。
- [`_on_infer_completed()`](src/core/DungeonLifecycleController.gd) → [`_apply_sub_critical(w, WILL_HEADROOM)`](src/core/DungeonLifecycleController.gd)：每條遺言**單獨**正規化方向、縮放到固定 L2 距離（`WILL_HEADROOM × EPSILON_C_APP`）→ 起始接近度恆 ~44%。
- 每週期開**全新 RL session**（`fixed_personality_vector` 只含該週期遺言）。

→ 「加減累積保存」目前不存在，需新建一個 **iterated** 條件；現制即天然的 **reset** 對照組。

（前端在 Windows repo `src/core/DungeonLifecycleController.gd`；後端在 WSL repo。架構見 `docs/前端架構_場景腳本與後端互動.md`。）

---

## 1. 收斂後的機制（三決策合一）

設 baseline 吸引子 **B**（`BASELINE_ATTRACTOR`，9D）、第 N 條遺言 SBERT 向量 **wₙ**、該遺言相對基線的偏移 **dₙ = wₙ − B**。

| 條件 | 第 N 週期人格 Pₙ |
|---|---|
| **Reset（對照＝現制）** | `Pₙ = apply_sub_critical(B + dₙ)` ＝ 只用本週期遺言 |
| **Iterated（處理）** | `Pₙ = apply_sub_critical(B + Σ_{k≤n} dₖ)` ＝ 累積到第 N 條的偏移和 |

- 兩條件**週期 1 完全相同**（S₁ = d₁）。
- `apply_sub_critical` 會把（vector − B）正規化方向、縮放到固定距離 ⇒ **兩條件每週期起始接近度都 ~44%**（決策 ③ 自動滿足）。
- 差異**只在方向**：週期 3 的人格指向「單一遺言 d₃」（reset）還是「d₁+d₂+d₃ 的累積方向」（iterated）。
- **連帶**：魯莽度（→事件強度/節奏）也用同一支向量算。Iterated 用累積向量算 `_compute_recklessness`，使「命運槓桿」與人格一致。

> ⚠️ 子決策（§9-A）：決策 ① 字面是「SBERT 向量疊加」。若**直接累加原始 wₖ**，SBERT 的共模（common-mode）成分會被乘 3 倍、淹沒各遺言訊號（`Σwₖ − B ≈ 3·共模 − B`）。**建議改成累加「偏移」Σ(wₖ−B)**（＝上表寫法），語意等同「疊加遺言對人格的淨加減」，且方向有意義。

---

## 2. 研究定位

- **新研究線**（暫稱 PI / Personality-Iteration），**重用既有 DLC 裝置**，不更動 P7-H H1（experiment/control 事件方向）已收的 N=27——兩者資料分開存。相關：[[p7h-apparatus-broken]]。
- IV 是 **Iterated vs Reset**（受試者間）。決策 ② 已排除「疊加在現有定向/隨機 A/B 上做 2×2」，故**事件方向因子在本研究中固定**（建議全體用 `experiment`＝定向，避免額外混淆與 N 膨脹；見 §9-B）。

---

## 3. 假設層級（**已於 2026-06-13 鎖定，見 §8d；本表為權威版，取代舊「主觀為 confirmatory」設定**）

| 層級 | 代號 | 內容 | 量測 |
|---|---|---|---|
| **PRIMARY confirmatory** | **E1** | Iterated vs Reset 在**玩家原始遺言的跨週期書寫一致性**上有差異（**雙尾、方向開放**） | cos(first,last) of will_sbert_vector（§4.2；兩臂計算對稱、客觀、行為） |
| Exploratory **floor-check** | q4 | 主觀**角色延續感**——manipulation-sufficiency probe（**預期兩臂落地板**，§8f） | 問卷題 q4_continuity（§4.4；主觀單題；null 為 informative 證據，非 failed hypothesis） |
| Exploratory descriptive | E2 | 累積方向是否改變後續週期崩壞/接近度軌跡 | RL 軌跡 per cycle×arm（§4.3） |
| **QA assertion（非假設）** | M_construction | Iterated 的 RL 種子方向跨週期錨定 > Reset | cos of will_personality_vector（§4.1；**套套邏輯：累積種子必然錨定**，僅驗管線有跑） |

> 改採 P7-H 前例（客觀 DV 為唯一 confirmatory、主觀降 exploratory）。**M 不是假設**而是 QA assertion（疊加種子必然錨定，無資訊量）。E1 是客觀行為 DV，**但方向開放**（見 §8d 誠實性聲明）。

---

## 4. DV 操作化（公式）

令 uₙ = 單位向量(Pₙ − B)。
- Reset：uₙ = normalize(dₙ)；Iterated：uₙ = normalize(Σ_{k≤n} dₖ)。

**4.1 M（錨定）**：`anchor = cos(u₁, u₃)`。預測 Iterated > Reset（reset 的 u₁,u₃ 來自獨立遺言 → 近正交）。亦可用逐步夾角均值 `mean(∠(u₁,u₂),∠(u₂,u₃))`，預測 Iterated 較小。

**4.2 E1（書寫策略）**：玩家原始偏移 dₖ 的連續相似 `cos(d₁,d₂), cos(d₂,d₃)`。方向不預設（建構 vs 轉向皆可能）→ 比較兩條件的分布/變異。

**4.3 E2（動態）**：每週期的 `rounds_survived`、接近度時間積分（AUC）、固定回合（如 r=50）接近度。做 cycle(1/2/3) × condition 的混合模型，看交互作用。

**4.4 H_PI（主觀）**：問卷新增
- `q4_continuity`：「這三段人生感覺像**同一個角色的延續**嗎？」（1–7）
兩條件都問（reset 組只是預期感受不到）。次要看既有 `q3_replay`、`q2_fun` 是否同向。

---

## 5. 流程（每位受試者）

1. 進場 → 系統指派 condition（iterated/reset，受試者間隨機）。
2. **三個週期**，每週期：寫遺言 → 分析（看雷達/命運傾向）→ 確認 → agent 自走 → 崩壞。
3. **告知框架（§9-C）**：建議在 iterated 條件以**遊戲內敘事**暗示累積（如「你過去的話語仍在回響」），讓操作在心理上生效；reset 條件不給此暗示。權衡：明示會引入 demand characteristics——故 H_PI 用**體驗題**而非「你覺得有沒有累積」這類引導題。
4. 三週期後 → 問卷（含 q4_continuity）。

---

## 6. 樣本量 / power（框架，非定數；**2026-06-13 更新：雙 DV variance estimation**）

- pilot 的任務＝**同時估 E1（primary）與 q4（secondary）兩個 DV 的變異**，各自回推達 power 0.80 所需 N，**取 max** 作為正式收案目標（不可只估 q4）。
- 兩樣本、雙尾、power 0.80：中效果 d≈0.5 → 約 64/組；大效果 d≈0.8 → 約 26/組。
- E1 雖客觀，但**方向開放**（§8d），pilot 效果量未知 → 必須實測其變異，不可假設「客觀＝低噪訊＝小 N」。
- q4 主觀單題，幾乎必 underpowered（與 P7-H「H2 is binding N constraint」一致）。
- M_construction 是 QA assertion（非假設），不參與 N 計算。
- **建議**：先跑 **pilot n≈10/組** → 同時取 E1、q4 變異 → N = max(N_E1, N_q4)。pilot 亦驗 reset 臂與現制資料等價（§10 鐵律 3）。

### 6b. Pilot 程序（收資料前鎖定 2026-06-13；forking-paths 紀律）

招募前先講好三件、不可事後更動：

1. **debrief blind-to-arm 編碼**：先定 rubric 三類——(A) 點到**敘事 priming**、(B) 點到**連續/累積機制**、(C) 都沒點到。收完**遮住 arm** 再依 rubric 編碼，**不可事後知道 arm 用眼睛判**。用途：若 B 類比例高（尤其 iterated）→ E1/q4 須打折（受試者察覺機制）；A 類高 → 強化「narrative-led」framing。編碼結果與 arm 解盲後才交叉。

2. **dropout buffer**：目標是**每臂 10 個「完整 3-cycle」run**（analyzer 把 `len(cycles)<3` 的 partial run **完全排除於所有 DV**——M/E1/q4/E2/saturation 皆只取 complete-3-cycle；2026-06-13 修正，先前 E1/M 會誤納 2-cycle partial 的 cos(c0,c1) 污染 primary。E2 再額外套 min_steps），不是 10 個 started。**每臂多招 2–3（~12–13 started）**吸收 dropout。count-balance 按 run_id 派發、partial 仍佔配額 → 多招才不會收完發現某臂不足 10。`selection.n_partial_runs_excluded` 會報被剔除數。

3. **N 回推用變異的 upper-CI / inflation factor，非點估計**：n=10 的變異很吵、E1 方向又開放，點估計 SD 會 under-power。改用 pilot SD 的 **upper-95% CI**（或對 variance 套 inflation factor）代入 N 公式。analyzer 已為此在 E1/q4 輸出 per-arm `dist={n,mean,sd,sd_upper95}`（§8d/analyzer）——**用 sd_upper95 算 N，取 max(N_E1, N_q4)**。

> 三項鎖定即可開招。資料進來跑 `analyze_iteration_study.py`，N 由本人依 sd_upper95 計算。

---

## 7. 分析計畫

- H_PI：Mann-Whitney U（q4 序數）或 t（若視為連續）；報效果量 + 95%CI。
- M：Iterated vs Reset 的 anchor 兩樣本檢定（預期 p 極小，僅作 manip check）。
- E2：linear mixed model，`proximity_AUC ~ condition * cycle + (1|participant)`。
- E1：兩條件 cos 分布比較（KS / 變異比）。
- 預先註冊 H_PI 為唯一 confirmatory，其餘 exploratory（控制多重比較）。

---

## 8. 實作改動（最小集合）

**8.1 DLC 加 condition 與累加器**（[DungeonLifecycleController.gd](src/core/DungeonLifecycleController.gd)）
- 新增 `var _iterate_personality: bool`（由分組決定，見 §9-B）。
- 新增 `var _will_accum_offset: Array = []  # Σ(wₖ − B)，整個受試者 run 持續，不在 start_next_cycle 清空`。
- `_on_infer_completed(vector)`：
  ```
  offset = vector − B                         # 本條遺言偏移
  if _iterate_personality:
      _will_accum_offset += offset            # 元素相加（首條時 = offset）
      eff = B + _will_accum_offset
  else:
      eff = vector                            # = B + offset，現制
  _will_personality = _apply_sub_critical(eff, WILL_HEADROOM)
  _will_recklessness = _compute_recklessness(eff)   # 動態也用累積向量
  ```
- `start_next_cycle()`：保持清 `_will_personality`（強制重寫），但**不**清 `_will_accum_offset`（迭代的核心）。
- run 開始（場景載入/受試者進場）時 `_will_accum_offset = []`（控制器本就每受試者新建，天然成立）。

**8.2 跨週期連結（必須，現在缺）**
現制每週期 = 獨立 player-test session（session_id = RL sid），**三週期無法被連回同一受試者**。需在 [PlayerTestSessionClient](src/core/PlayerTestSessionClient.gd) 的 `/player-test/start` payload 加：
- `run_id`（受試者進場時生成一次的 UUID，三週期共用）
- `cycle_index`（0/1/2）
- `iteration_arm`（"iterated"/"reset"）
- `will_cumulative_offset`（該週期的 Σ(wₖ−B)，供事後重建軌跡）
後端 `/player-test/start` 與持久化 schema 需接住這些欄位。

**8.3 問卷加題**：`/survey/questions` + `/survey/submit` 加 `q4_continuity`（前端 SurveyPanel 已是動態建題，主要改後端題庫與 schema）。

**8.4 分組來源**：見 §9-B。

> 改的都在前端 DLC + PlayerTestSessionClient + 後端 player-test/survey schema；**不動** RL 管線、proximity 定義、apply-event 機制。後端改完記得**手動重啟**（[[backend-restart-required]]）。

---

## 8b. 落地狀態（2026-06-13）

§8.1–8.3 **已實作 + 離線驗證通過**（後端 48 tests green + 邏輯檢核；前端 GDScript parse smoke 0 error）。
- 前端：`DungeonLifecycleController`（`_will_accum_offset` 累加器、`_on_infer_completed` 用 iterated/reset 有效向量、`_gen_run_id`、start_session 帶 run_id/cycle_index）；`PlayerTestSessionClient`（run_id/cycle_index/iteration_arm 欄位、assign 帶 run_id、capture iteration_arm、start payload +3 欄位、submit_survey +q4）；`SurveyPanel`（q4 slider）。
- 後端：`ab_test_manager`（run_id-gated sticky 計數平衡 `iteration_arm` + save/load 持久化；legacy assign 完全不變）；`player_test_tracker`（dataclass +3 欄位）；`survey_manager`（q4_continuity 題 + 欄位 + 驗證，舊 3 題客戶端送 0 相容）；`server.py`（request models + forwarding）。
- **累加器設計要點**：每週期都累加偏移（與 arm 無關），arm 只決定人格用「累積」還是「單條」→ 即使 iteration_arm 在週期 1 尚未由後端回傳，週期 2 的 Σ 也已備妥；reset 永遠等價於改動前行為。
- **未做**：① 決策 C 敘事 priming 文案（iterated 組過場「你過去的執念仍在深淵回響…」）—— iteration_arm 已可由 `PlayerTestSessionClient.iteration_arm` 取得，待接到 V2 過場/WillScreen。② **重啟 live 後端**（:8000）才會載入新碼（[[backend-restart-required]]）—— 屬研究裝置狀態變更，待確認後重啟。③ Pilot 收案。

## 8c. Reviewer 回合修訂（2026-06-13）

**P1 — pre-reg 邊界聲明（confirmatory 的因果範圍）**：
> 因 iterated 組在第 2/3 週期開場加入敘事 priming（「你過去的執念仍在深淵回響……」），而 reset 組顯示「新的人生開始了……」**主動植入 discontinuity framing**，**H_PI(q4_continuity) 量到的是「機制迭代 ＋ 敘事框定」的聯合處理（joint treatment），不是純機制效應**。reset 的 fresh-start 文案會放大 q4 對比。這是刻意的設計選擇（matched manipulation：兩臂都給過場、只變內容不變有無），不是 bug。**Writeup 不得宣稱「迭代機制本身造成 felt continuity」**；只能宣稱「迭代機制＋其敘事框定的聯合處理提升延續感」。要分離機制 vs 敘事需做 2×2（mechanism × narrative）——**pilot 階段不做**，維持聯合處理 + 誠實標註。

**P2 — M 改為兩條 cosine（will 文字確認為每週期自由重寫、空白框、無 seed/prefill）**：
- `M_construction_check`＝cos(first,last) of **will_personality_vector**（縮放後 RL 種子）。iterated 因種子是累積 B+Σdₖ → **anchored BY DESIGN**，僅作「累積管線在 production 真的有跑」的構造檢核，**非行為發現**。
- `E1_will_writing_behavior`＝cos(first,last) of **will_sbert_vector**（玩家每週期自由重寫的原始遺言；兩臂計算完全相同）→ 不繼承構造錨定，是真正的行為測量（迭代框定是否改變玩家書寫一致性）。
- 合成驗證：M iterated 0.65 vs reset −0.23（構造錨定可見）；E1 兩臂皆 ≈ −0.24（獨立遺言、無構造優勢）→ 兩者確實分離。

**P3 — survey 確認為 run-level**（一次/run，於 SURVEY 狀態提交，附最終週期 session_id）。analyzer 的 q4 join 已改為**明確取最大 cycle_index 的 session**，不靠遍歷順序。

**P4 — dogfood 通過（隔離後端，未污染 live 研究資料）**：headless 驅動真實前端類別（DLC→PlayerTestSessionClient→backend）跑 2 個完整 run（count-balance 自動得 1 iterated + 1 reset，各 3 週期 + run-level q4），指向隔離 `P7H_OUT_DIR=reports/experiments/iter_dogfood`。產出 6 sessions：run_id 一致、cycle_index 0/1/2、iteration_arm 正確、group=experiment（決策 B）、will 向量齊全；2 筆 survey 各 q4=9 附最終週期。analyzer 在此真實資料上輸出 n=1/arm、complete 3-cycle、0 legacy，M/E1/q4 皆計算（n=1→正確報「insufficient N」不崩）。**結論：record 產出端到端正確。** 註：此為「程式碼路徑」dogfood（真實類別、腳本化輸入），**未驗證 GUI 視覺操作**（按鈕/slider 渲染與點擊）——招募前建議真人做 1 次視覺 playthrough。完成後 live 後端已還原（PID 1480350，178 sessions，未含 dogfood 的 6 筆）。

## 8d. Confirmatory hierarchy — LOCKED 2026-06-13（pilot 解盲前鎖定，防 forking-paths）

拆 M 為 construction/E1 後 confirmatory hierarchy 改變，於 pilot 收案/解盲**前**鎖定如下（仿 P7-H：客觀唯一 confirmatory、主觀降 exploratory）：

1. **M_construction_check ＝ QA assertion，不是假設**。iterated 種子是累積 B+Σdₖ → 方向必然錨定，純套套邏輯；只用來確認「累積管線在 production 真的有跑」。**不計入 power、不作發現宣稱。**
2. **E1（raw-will SBERT 錨定）＝ PRIMARY confirmatory**。客觀、行為、兩臂計算對稱。檢定為**雙尾、方向開放**（見下誠實性聲明）。
3. **q4_continuity ＝ secondary / exploratory**。主觀單題、pilot 必 underpowered；報效果量+CI，不設 go/no-go。**（2026-06-13 §8f 進一步降為 EXPLORATORY floor-check：預期兩臂落地板，此 null 為 informative；以 §8f 為準。）**
4. **E2 dynamics ＝ exploratory descriptive**。

**誠實性聲明（必須隨結果一起報）**：
- **E1 方向是開放的**：pilot 可能無差異、甚至反向。可能機轉：reset 玩家若價值觀穩定，會自然寫出一致的連續遺言（高 cos）；iterated 玩家若被「執念回響」框定，反而可能刻意求變（低 cos）。故不預設 iterated>reset。
- **E1 與 q4 同為「機制迭代 ＋ 敘事框定」的聯合處理**（與 §8c-P1 同源）：reset 的 priming 抑制、iterated 的 priming 強化，皆混入。**純機制的行為 DV 不存在**——要分離機制 vs 敘事須做 mechanism×narrative 的 2×2，**已 defer，pilot 不做**。
- 因此即使 E1 顯著，writeup 僅能宣稱「聯合處理改變書寫一致性」，不可宣稱「迭代機制本身造成行為改變」。
- **機制視覺上近乎不可見（code 確認 2026-06-13）**：V2 唯一隨臂別變化的元素是 priming 文字行；雷達基準用「本週期單條遺言」(infer_preview_ready 發 raw `vector`)，**兩臂、各週期都一樣**，不洩漏累積；無任一 HUD 元件「iterated 有、reset 沒有」。故玩家**感受到的**差異幾乎全是敘事 priming（累積只暗藏在 RL 事件節奏/強度，靠魯莽度由累積向量算）。這進一步壓縮「純機制」的可宣稱空間。

## 8e. 真人 GUI playthrough gate — demand-characteristics 視覺稽核（招募前必過）

程式碼路徑 dogfood 已過（§8c-P4），但 GUI 視覺未驗。招 pilot 前由真人在 live build **各走一遍 iterated + reset run**（3 週期 + 結尾問卷），明確稽核四項：

1. **priming line**：cycle 2/3 確實 render，內容各臂正確（iterated「你過去的執念仍在深淵回響……」/ reset「新的人生開始了……」），cycle 1 無。**靜默不 render＝處理變 null 且資料看不出來** → 這是 manipulation 本體，必查。
2. **will 輸入框每輪視覺為空**（無預填；對照 code：start_next_cycle 不 seed、reset() 清框）。
3. **結尾問卷**：q4_continuity slider **與** manipulation-awareness 開放題（§8e-B）都出現且可作答。
4. **臂別洩漏稽核（已收窄至單一 tell）**：code-side 已清掉除 priming line 外所有 arm-conditional 元素（雷達兩臂同顯單條遺言、不洩漏 cumulative、無臂別專屬 widget——§8d 預檢）。故真人這一遍**只剩一個要盯的 tell**：**iterated 的跨 cycle 事件節奏升級（recklessness 由累積向量變大 → 事件更頻繁/更強）是否 salient 到 naive 受試者能察覺自己「這輩子比上輩子更猛」**。若明顯 → 記入限制（可能洩漏臂別/強化 priming）；若 naive 受試者察覺不到 → 過關。

> 第 1–3 項照常驗；第 4 項聚焦 pacing tell。任一項不過 → 修正後重走，**過了才招募**。

## 8e-B. Pilot 結尾 manipulation-awareness debrief（已實作 2026-06-13）

問卷尾端新增開放題（funnel-style，放最後避免污染前面評分）：「你覺得這個研究在測什麼？過程中有沒有感覺到什麼是被刻意設計來影響你的？」→ 落 `survey.manipulation_awareness`。**若多數受試者答中操弄（尤其敘事 priming）→ E1/q4 須打折解讀**。迭代研究有 narrative priming，此題為必要保險（P7-H 無、此處新增）。已端到端落地：survey_manager QUESTIONS(type=text) + SurveyResponse + submit；server schema；PlayerTestSessionClient.submit_survey 第 9 位置參數；SurveyPanel 依 type 渲染多行文字框。

## 8e-C. GUI audit 結果 + limitations（2026-06-13；audit PASS、pilot green-lit）

真人各走 iterated + reset 一遍：① priming 兩臂正確 render（reset「新的人生開始了」/ iterated「你過去的執念仍在深淵回響」）✓ ② will 框每輪空 ✓ ③ 結尾問卷（4 slider + debrief 文字框）出現 ✓ ④ pacing tell 改用軌跡資料客觀判定（見下）。**4 項全過 → green-light pilot n=10/arm（含 debrief）。**

**Limitation L1 — pacing tell 是 direction-dependent / saturating**：item 4 用 analyzer 讀軌跡客觀判定（非主觀體感）。實測：proximity per-step climb-rate 兩臂相近（iterated 0.005–0.0066、reset 0.0085–0.0095，皆 ~同數量級），max_proximity 都撞天花板 ~1.0 → **無明顯「越來越猛」escalation tell**（這位玩家反而越來越 calm）。**但**：tell 方向由玩家遺言決定（n=1 這位 saturate 到 calm；若玩家寫 wild-escalating 遺言，cumulative 會 saturate 到 max wildness（cadence 6 / intensity 1.8），屆時可能變 salient tell）。**安全網＝pilot debrief（manipulation_awareness）監測是否有人察覺**。

**Limitation L2 — cumulative-recklessness saturation（實質發現，非 bug）**：iterated 的 recklessness 由 *cumulative* will offset 過 sigmoid(k=3) 算，wills 一疊就釘到極端，cycle 2–3 dynamic range 崩掉（這位玩家 0.358→0.058→0.005，cycle 1&2 幾乎一樣：intensity 0.67 vs 0.61、cadence 都 14）；reset 維持 spread（1.33/1.00/0.73，獨立遺言）。

**機制凍結（binding）**：**pilot 前不動 sigmoid / 不 cap / 不 renormalize**。saturation 很可能是「執念釘死在極端」的設計意圖本身；cap/renorm 留給「機制顯化 v2」當 design question，**現在只 flag 不改**。

**Analyzer 已把 saturation 收為 measured covariate（2026-06-13）**：
- E2 改用 **per-step climb-rate** 當 arm 區辨量（取代天花板效應的 max_proximity）。
- 新增 `saturation_covariate_exploratory`：per run（兩臂都算）`sat_index = 1−2·min(r,1−r)`（r＝最終週期 recklessness；高＝cumulative 把人格釘到 sigmoid 極端＝機制劑量大）。
- **within-iterated dose-response**：iterated 臂內敘事為常數、saturation（機制劑量）為變數 → `sat_index × E1`、`sat_index × q4` 的 Spearman（n≥3 才算）。**正斜率＝機制在常數敘事之上有貢獻**，初步碰 claim ceiling、決定 v2 值不值得。**標清：observational、非隨機、與 trait-consistency 混淆 → exploratory only，不取代 mechanism×narrative 2×2。**

## 8f. q4 角色重定義 + 問卷精簡 + v2 解耦 mandate（2026-06-13；受試者第一手回饋觸發）

開發者以**受試者身分**親走後回報：三週期**感覺不到同一個角色**（性格與 event 判斷的跨週期關係體現不出來），debrief 自述「像是按個性做 event 選擇，崩壞後又有新人生」——即**未察覺操弄**。這是 §8d/§8 framing「機制視覺隱形」的第一手實證。據此鎖定三件事，**取代 §8d-3 與 §9-D 的「q4=secondary」設定**：

**1. 問卷精簡——移除 q1_naturalness / q2_fun。** 兩題為 P7-H 舊建構（特質漂移自然度／接近度邊界張力），與迭代研究無關，受試者普遍看不懂。**資料模型欄位保留**（相容既有 20 筆回應 + legacy），僅從 `/survey/questions` 題庫移除不再呈現。現行問卷＝`q3_replay`（UX）＋`q4_continuity`（floor-check）＋`manipulation_awareness`（debrief）。

**2. q4 從 secondary 降為 EXPLORATORY floor-check（manipulation-sufficiency probe）。** 機制隱形 → q4 唯一可被推動的途徑是**那一句過場 priming**。因此 q4 的角色改為：「純敘事 priming 到底能不能撼動 felt continuity？」
- **預期兩臂都落地板（floor）。此 null 是 INFORMATIVE，不是 failed hypothesis。**
- 它把開發者 n=1 自述升級成 **n≈20 可引用證據**：純敘事操弄不足以產生延續感 → 構成「建解耦式 continuity v2」的**實證 mandate**。
- pre-reg 明文：q4 預期 null 標為 informative；**全砍 q4 會丟掉這個證據**，故保留題目文字、保留收集，僅改解讀層級。
- analyzer 已對應改名 `q4_continuity_floor_check_exploratory` + `sat_vs_q4_floor`，輸出帶 expected-null-informative 註解。

**3. 「機制顯化 v2」的致命陷阱 + 解耦 mandate（記下，防 v2 重蹈覆轍）。** 先前 Option-2 草案「在 cycle 2/3 開頭顯示前世累積人格（雷達預載 B+Σ／『你的前世人格：X』摘要）」**有致命缺陷**：顯示人格向量 X 會讓玩家**照 X 寫遺言** → E1 錨定變成**人工產物（tautology）**，與當初「carry 遺言文字會 tautologize M」**同一失效模式**。故：
- **v2 的 continuity 線索必須與「遺言／人格向量」通道徹底解耦。** 可用：世界狀態持續、重複出現的 NPC、「第三條命」的敘事框定、前世遺言**主題**（非向量）的 callback——**絕不顯示人格向量、絕不把人格回灌進書寫錨點**。
- v2 是 pilot **之後**的獨立 study，現在不做。

**決定性理由（為何 pilot 必須現在跑、不等 v2）**：E1 的乾淨自由書寫**只有在機制隱形時量得到**；任何「可見機制」的 v2 都會把人格訊號回灌進書寫，污染 E1（套套邏輯）。因此**這個 narrative-led pilot 是唯一一次能取得乾淨 E1 的窗口**，必須在做 v2 之前先跑完。

**順序（不變，q4 角色更新）**：砍 q1/q2 → q4 降 exploratory 當 floor-check → 跑 pilot（乾淨 E1 主結果 ＋ q4 floor 證據 ＋ debrief ＋ saturation dose-response）→ 用結果**授權並設計解耦式 continuity v2**。

## 8g. naive vs 實驗者判別 + 乾淨 pilot 邊界（2026-06-13；provenance 稽核觸發，pre-registered）

**病灶**：`player_alias`（前端 hardcode "player"）與 `is_human`（前端 hardcode `true`）**都分不開 naive 受試者 vs 實驗者試玩**。provenance 稽核發現 live `p7h_real_study` 裡 15 個 iteration run（45 sessions）**全是實驗者自玩**（§8e audit + dev 測試，2026-06-13 09:31→06-14 03:27），0 naive；唯一 q4=3.0 是實驗者本人填（知道操弄→confirmation bias，丟棄）。isolation 本身守住（#2 的 26 sim 在已刪的 iter_qa、dogfood 2 run 在 iter_dogfood，run_id 交集=NONE；20 筆 wsim 是 commit 在案的原 P7-H sim，無 run_id，analyzer 本就排除）。

**機制（pre-registered，權威判別子＝participant_id 非 timestamp）**：
- 新增 `participant_id` 欄位（session model + `/player-test/start` payload + 前端 client/controller）。**naive 受試者用分配代碼 `P01…`（含 dropout buffer 招 ~12–13/臂）；實驗者試玩永遠用保留值 `dev`**。start screen 加小文字欄，預設 `dev`，實驗者交給受試者前填入該人代碼；run-level、僅第 1 週期顯示（不可中途改），即時同步給 controller。
- **analyzer 權威過濾＝participant_id ∈ pilot allowlist**（預設樣式 `^P\d{2,}$`，或 `--pilot-participants P01,P02,…` 明確清單）。`dev`/`EXP_PREPILOT` 永不符 → 排除。**次要保險**（非權威）：run 內 `is_human` 全 True **且** `started_at ≥ pilot_start`（`--pilot-start` ts）。為何不能只靠 timestamp：relaunch 後實驗者試玩會落在 pilot_start 之後被誤計 → **必須正向標記 naive**。
- **既有 15 run quarantine**：用 tag 不移檔（移檔會被 backend flush 覆寫，且同檔有 74 legacy + 20 wsim，動結構有風險）。停 backend → 既有 45 iteration sessions 回填 `participant_id='EXP_PREPILOT'` → 重啟。analyzer 自動排除、留作參考。已執行（2026-06-13，先備份 `_prepilot_bak/`）。
- **程序紀律（招募期）**：實驗者試玩**永遠** `dev`、naive **永遠**用分配代碼 → 即時交錯也安全（不靠時間窗）。

**驗證（2026-06-13）**：923 tests green；analyzer 預設樣式 → naive=0、15 run 排除為 `EXP_PREPILOT`；`--pilot-participants EXP_PREPILOT` → 該 15 run 納入（iterated=8/reset=7）證明 allowlist **納入路徑**正常（真 P01… 進來會被計）。`selection.n_naive_pilot_runs` + `participant_gate.{allowlist,n_runs_excluded_by_reason,excluded_runs}` 報明細。**招募前狀態：0 naive、apparatus 就緒。**

**count-balance 配臂 = started（非 completed），且配臂計數同樣用 participant_id 過濾（2026-06-13）**：
- `_assign_iteration_arm` 在 run 第一次 assign（cycle 1）時配臂，平衡計數數所有 started run（含 dropout）。→ **停止規則必須綁 completed**：招到「兩臂都 ≥10 completed」，期間看 `selection.n_complete_3cycle_runs`，預期每臂 started ~12–13（真人有 dropout，不會像 sim QA 的 12/12 對稱）。
- **不清池（rejected）**：一次性清到 0/0 擋不住招募期交錯的 dev 試玩（清完玩一次池又變 1/0，動態污染）。**正解＝配臂計數只數 pilot-eligible run**（`participant_id` 配 `^P\d{2,}$`，與 analyzer gate 同源）；`dev`/`EXP_PREPILOT` 仍配到臂（試玩能跑）但**不計入平衡 tally**。非破壞性（EXP_PREPILOT 留檔當參考、不冒 flush 風險）、naive cohort 內部自我平衡、扛得住交錯 dev、assignment 與 analysis 同源。
- 工程：`participant_id` 經 `/bifurcation/ab-test/assign`（payload + `ABAssignRequest` + 前端 assign payload）帶入；`ABTestManager._iteration_pid: dict[run_id→pid]` 持久化（save/load；舊存檔無此鍵→get 預設 "dev"→15 個 EXP_PREPILOT 自動不計）。
- **驗證**：(a) EXP_PREPILOT 在場 + dev 試玩後，pilot tally 仍 0/0、dev 不進池，P02–P06 在 P 碼間自我平衡（3/3）；(b) P01 冷啟動 pilot 池空 → hash 配臂、跨 cycle sticky。925 tests green（新增 2 test 鎖此行為）。

## 9. 子決策（已拍板 2026-06-13）

- **A → 偏移累積 Σ(wₖ−B)**（已實作）。
- **B → 後端 assign 派發 iteration_arm（run_id-gated，sticky/計數平衡）+ 事件方向固定 experiment**（已實作；run_id 模式回 group="experiment"，不污染 legacy control/experiment 平衡）。
- **C → 敘事 priming**（iterated 組過場氛圍暗示；reset 維持標準）—— 待接文案。
- **D → confirmatory = 主觀延續感 q4；E2(proximity_AUC) 為 co-primary**。先跑 pilot n=10/組，若主觀方差過大則依 P7-H 前例把客觀動態升為 confirmatory、主觀降 exploratory。**（已 superseded：§8d 鎖定 E1=唯一 PRIMARY confirmatory；§8f 再把 q4 降為 exploratory floor-check。以 §8d/§8f 為準。）**

## 9b. 原待定子決策（保留紀錄）

- **A. 累加用「原始 wₖ 和」還是「偏移 Σ(wₖ−B) 和」？** 建議偏移（§1 註）；否則共模淹沒訊號。
- **B. condition 怎麼指派？** (a) 後端 `/bifurcation/ab-test/assign` 多回一個 `iteration_arm`（與現有 group 並行，最乾淨、可追蹤）；(b) 啟動參數/場景旗標（快但難隨機追蹤）。建議 (a)。事件方向因子是否固定在 `experiment`？建議是。
- **C. 是否告知玩家「遺言會累積」？** 建議用遊戲內敘事暗示（非說明書式），並用體驗題避免 demand。你也可選完全不告知 → 改測「玩家是否自行發現」（更乾淨但效果更弱、N 更大）。
- **D. confirmatory 要不要改？** 目前設主觀延續感（最貼合設計目的、但 N 貴）。若你想要便宜且客觀的 confirmatory，可改用 E2 動態其中一項，主觀降為 exploratory（仿 P7-H 把 H2 降級）。

---

## 10. 回歸/等價檢查

- [ ] Reset 條件產出的 `p7h_player_test_sessions` 欄位/數值與現制等價（除新增 run_id/cycle_index/arm 欄位）。
- [ ] 兩條件週期 1 的 `_will_personality` 完全相同（S₁=d₁）。
- [ ] Iterated 每週期起始接近度仍 ~44%（決策 ③）。
- [ ] 三週期可由 run_id 正確連回同一受試者。

相關：[[p7h-apparatus-broken]] [[v2-immersive-ui]] [[frontend-architecture-doc]] [[backend-restart-required]]
