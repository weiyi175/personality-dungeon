# 人格生態評分系統（Personality Ecology Scoring）規劃 v1

> 起點：使用者構想「每場冒險評分（目前**還沒有**）→ 上傳冒險經驗至伺服器（`.json` 已存在）→ 比對全體玩家資料 → 若多數玩家走相似人格發展則**降分**、並**動態調高其他人格的評分權重**使其更有優勢 → 達成伺服器人格收集數據的**平衡** + 達成 **RPS L3 旋轉**」。
>
> 釐清（2026-06-12 對話，見 [[p7h-apparatus-broken]]）：現制的 L1/L2/L3（`analysis/cycle_metrics.py`）判定的是**模擬基質層**——單一 session 內 300 個 RL 代理人的「三策略比例」在單純形上的旋轉，與真人玩家／人格型態**完全沒有接線**。本構想是把**同一套複製動力學上移到 meta 層（跨玩家伺服器生態）**。這份文件把它收斂成「可實作的機制 + 可判定的指標」，並**重用** `cycle_metrics` 的 L0–L3 分類器與 `evolution/independent_rl.py` 的循環 payoff 結構。
>
> 落地前請先拍 **§1 的岔路**與 **§7 的子決策**。
>
> **決策已定（2026-06-12）**：§1 岔路 = **路 B（旋轉生態，循環克制）** —— 直接做 RPS 旋轉，不走 A-now-B-later 過渡。隨之預設（可後續微調）：§7-B=**3 原型**（路 B 強制）、§7-C=**冒險終態 mean_personality**、§7-D=**每 N=10 筆上傳一個 bin**、§7-F=**不設 target，權重由循環 fitness 驅動**。§7-E（誘因）對路 B 是**必要**的（無實質誘因則生態不會旋轉），後端先把 score + advantage 算好回傳，實質獎勵由前端旗標決定。

---

## 0. 現況 gap（為什麼這是全新一層）

三個現有層級彼此不接：

| 層 | 量什麼 | 在哪 | 與本構想的關係 |
|---|---|---|---|
| 基質層 RPS | 單 session 內 300 代理人的三策略（aggressive/defensive/balanced）旋轉，分 L0–L3 | [analysis/cycle_metrics.py](analysis/cycle_metrics.py) + [evolution/independent_rl.py](evolution/independent_rl.py) | 是「複製動力學引擎」，但只活在模擬內 |
| 量測層 P7-H | 單場冒險的人格分岔接近度（玩家 9D 漂移多接近崩壞）| [api/player_test_tracker.py](api/player_test_tracker.py) | DV 完全不同；裡面**沒有 cycle_level 一行** |
| **生態層（本文，未建）** | 跨玩家人格原型佔比隨時間的演化 + 頻率依賴評分 | 不存在 | ← 本文要新建 |

→「評分 / 比對 / 頻率依賴 / 生態判定」在代碼裡**目前完全不存在**。本文新建生態層，但**不重造輪子**：分類器與循環 payoff 直接重用。

---

## 1. ⚠️ 岔路拍板（最先決定，決定整個系統的形狀）

使用者同時要「平衡」與「L3 旋轉」，但兩者是**不同機制、且不能同時成立**：

| 路 | 機制 | 收斂行為 | cycle_level | 服務的目標 |
|---|---|---|---|---|
| **A：靜態平衡** | 純稀有度（懲罰原型**自身**普及度）| 收斂到單純形中心的**靜止點** | **L0** | 「資料庫平衡」「防單一人格壟斷」 |
| **B：旋轉生態** | 循環克制（被**剋星**普及度壓分）| **極限環**（佔比永遠在轉）| **L1 → L3** | 「RPS 旋轉」（長期時間平均 = 平衡）|

**關鍵事實**：完美平衡的靜止點**不在轉**。L3 的「平衡」是**時間平均**平衡、任一瞬間都有一個型在領先（攻擊型領先→防守型→平衡型→循環）。你不能同時要「靜態平衡點」又要「持續旋轉」。

**硬現實**（[研發日誌 §B-series 負結果](研發日誌.md)，約 L2375/L2385）：連**乾淨的後端模擬**——在 locked working point 的 well-mixed sampled replicator——即使引入 state-dependent k / stratified growth / tangential drift 三種機制，**仍穩定收斂回 Level 2 plateau，達不到 L3**。要靠稀疏、自選擇、有噪音的幾百筆真人上傳**自發**出現 L3，是非常樂觀的。

**建議：A-now-B-later。** 先做路 A（必定收斂、直接服務你的主目標），把路 B 當探索性 stretch goal。L3 設為**探索儀表**而非 KPI。

> **更正（2026-06-12）**：原稿曾寫「A 是 B 的退化特例，cross=0 時 B→A」——**此說有誤**。循環旋轉來自 payoff 的 `[[0,a,−b],[−b,0,a],[a,−b,0]]` 基本結構（a,b 本身就是 RPS 環），`cross=0` 仍會旋轉。路 A 與路 B 是**不同的 fitness 規則**：路 A `fitness_i = −q_i`（只懲罰自身普及）、路 B `fitness_i = Σ_j A[i][j]·q_j`（循環克制）。要切回路 A 得換規則、非調 cross。本專案既選路 B，採後者。

---

## 2. 型態降維（玩家 9D → 生態原型）

生態分類器需要「少數幾個原型、佔比和為 1」的時間序列。9D 直接當 9 類太稀疏、且 9 類難設循環克制。

| 選項 | 優點 | 缺點 |
|---|---|---|
| 9 職業（現有 [CLASS_BY_DIM](src/ui/DungeonLifecycleSceneV2.gd)，主導維度衍生）| 細緻、敘事豐富 | 9 類稀疏、循環矩陣難定義 |
| **3 原型（RPS 對齊，建議）** | 可**直接重用** `strategy_payoff_matrix` 的循環結構；不稀疏；與基質層語意一致 | 損失細粒度 |

**建議 3 原型**，由 9D 線性投影定義（係數待 §7-B 校準）：

```
FEATURE_NAMES = [impulsiveness, assertiveness, optimism, risk_aversion,
                 suspicion, endurance, randomness, stability_seeking, curiosity]

s_aggressive = impulsiveness + assertiveness − risk_aversion
s_defensive  = risk_aversion + suspicion + endurance + stability_seeking
s_balanced   = optimism + curiosity − |extremity|        # 中庸傾向

# 硬分類（記 archetype）或軟權重（記 soft proportions，建議軟）：
q = softmax([s_aggressive, s_defensive, s_balanced] / τ)   # Σq = 1
archetype = argmax(q)
```

> 與 P7-H 既有的「魯莽度 R」一致性：R 高 ≈ aggressive、R 低 ≈ defensive，可拿來交叉驗證投影是否合理（§9 回歸檢查）。

---

## 3. 評分公式

設時間窗 *t* 的 DB 原型佔比 **q(t) = (q_agg, q_def, q_bal)**，Σ=1；本場冒險玩家原型 *i*（硬或軟）。

### 3.1 路 A — 稀有度（負頻率依賴於自身）

```
score = base · (1 − q_i)            # 自身越普及分越低
# 或正規化版（避免線性飽和）：
score = base · (1/3) / (q_i + ε)    # q_i = 1/3 時得 base；稀有型 > base、熱門型 < base
```

### 3.2 路 B — 循環克制（負頻率依賴於剋星）

重用 [independent_rl 的 payoff](evolution/independent_rl.py#L40)：`A = [[0, a, −b], [−b, 0, a], [a, −b, 0]]`（+ cross 耦合）。原型 *i* 的「生態適應度」：

```
fitness_i = Σ_j A[i][j] · q_j       # 被剋星 j 普及時 A[i][j]=−b 壓分；剋制對象普及時 +a
score = base · softplus(fitness_i)
```

（注意：此式的旋轉來自 `[[0,a,−b],…]` 環結構本身，與 §1 更正一致；要切回路 A 須改用 §3.1 的 `−q_i` 規則，非調 cross。）

### 3.3b 實質獎勵：評分區間 → 金幣（v1 粗劃，使用者決策）

評分轉金幣（[ecology_tracker.py](api/ecology_tracker.py) `COIN_BRACKETS`）。**v2 區間**經 [ecology_will_replay.py](scripts/experiments/ecology_will_replay.py)「方案 S」優化（46 真人 + 90 合成遺言實測）：200 檔語意保留＝分數破百（搭上稀有剋星，旋轉行為，稀有）；其餘 4 檔對 <100 分數分位數均衡：

| score ≥ | 金幣 | 語意 |
|---|---|---|
| 100 | 200 | 搭上熱潮（選了當前最吃香的稀有剋星，分數破百）|
| 82 | 100 | 高分 |
| 76 | 50 | 中位帶 |
| 64 | 25 | 中下 |
| 0 | 10 | 墊底（人格類型過度普及）|

v2 落點均衡：10💰×34 / 25💰×36 / 50💰×35 / 100💰×31 / 200💰×0（200 在混合遺言 replay 為 0，上線生態傾斜後才觸發）。`coins` 欄位**落檔保存**（EcologySubmission.coins），`/ecology/submit` 回傳；前端 CollapseScreen 顯示「💰 獲得 N 金幣（本局累計 M）」。**尚無持久錢包**，累計只在單一受試者 run 內（V2 場景 `_total_coins`）。重新優化區間：跑 `ecology_will_replay.py`。

### 3.3 動態權重回饋（＝你說的「動態調高其他人格的評分權重」）

用離散指數權重複製動力學（與 [evolution/replicator_dynamics.py](evolution/replicator_dynamics.py) 同款）：

```
w_i(t+1) = w_i(t) · exp(η · (target_i − q_i(t)))   # 普及型 w 衰減、稀有型 w 升高
final_score = score_i · w_i(t)
```

- 路 A：`target_i = 1/3`（拉向均勻）。
- 路 B：**不設 target**，讓循環 payoff 自己驅動旋轉（設 target 會把環壓回中心、殺掉 L3）。
- 閉環：稀有型 CP 值高 → 下一批玩家傾向選稀有型 → 該型變普及 → 分數/權重回落 → 負頻率依賴成立。**前提是評分要對玩家有實質誘因（§7-E）**，否則玩家不會回應，閉環不啟動。

---

## 4. DB schema（伺服器持久化）

新建獨立 store（與 `p7h_player_test_sessions.json` 分開，避免污染 P7-H 研究資料）。兩種 record：

**(a) 每筆上傳的冒險經驗**
```json
{
  "run_id": "uuid", "session_id": "uuid", "ts": 1234567890.0,
  "archetype": "aggressive|defensive|balanced",
  "archetype_soft": [0.6, 0.25, 0.15],
  "personality_9d": [/* 來源 9D：建議冒險終態 mean_personality，見 §7-C */],
  "score": 0.0,
  "score_components": {"rarity": 0.0, "fitness": 0.0, "weight": 0.0},
  "outcome": {"max_proximity": 0.0, "rounds_survived": 0}   // 連結 P7-H DV，可做交叉分析
}
```

**(b) 生態快照（時間序列，餵 classify_cycle_level）**
```json
{
  "bin_index": 0, "ts": 1234567890.0,
  "counts": {"aggressive": 12, "defensive": 9, "balanced": 14},
  "proportions": [0.34, 0.26, 0.40],
  "weights": [1.0, 1.08, 0.93]
}
```

**API（新增，仿 P7-H 端點風格）**
- `POST /ecology/submit` — 上傳冒險 → 投影 archetype → 算 score → 更新 counts/weights → 回傳分數 + 當前生態。
- `GET  /ecology/snapshot` — 當前佔比 / 權重（不推進）。
- `GET  /ecology/assess` — 跑 `classify_cycle_level` → 回 L0–L3 + stage scores（生態健康儀表板）。

持久化沿用 P7-H 的記憶體 singleton + JSON save/load 模式（注意：每次寫整檔重寫、startup 用 `load()` 載回，要改資料須停 server→改→重啟，見 [[backend-restart-required]]）。

---

## 5. 生態判定接線（重用 cycle_metrics，**不改一行**）

分類器簽章（[cycle_metrics.py:1345](analysis/cycle_metrics.py#L1345)）：
```python
classify_cycle_level(
    proportions: Mapping[str, Sequence[float]],   # {"aggressive":[...], "defensive":[...], "balanced":[...]}
    *, burn_in=0, tail=None, amplitude_threshold=0.02, ...
) -> CycleLevelResult(level, stage1, stage2, stage3)
```

接線（`GET /ecology/assess` 內）：
```python
# 1. 從生態快照(b)蒐集每原型的佔比時間序列
series = {
    "aggressive": [snap.proportions[0] for snap in snapshots],
    "defensive":  [snap.proportions[1] for snap in snapshots],
    "balanced":   [snap.proportions[2] for snap in snapshots],
}
# 2.（可選）normalize_simplex_timeseries 確保逐步 Σ=1
# 3. 分類
result = classify_cycle_level(series, burn_in=BURN, tail=TAIL)
return {"level": result.level, "s3_score": ..., "n_bins": len(snapshots)}
```

判讀：
- **L0** = 壟斷或靜止 → 路 A 達標**也是 L0**（這是預期，不是失敗）。
- **L1/L2** = 生態在循環/振盪 → 路 B 的中繼里程碑。
- **L3** = 真旋轉 → stretch goal；參考 §1 的 L2-plateau 負結果，**列為探索儀表、非驗收 KPI**。

⚠️ `cycle_metrics` 是為「幾千 round 的**密集**序列」設計的。跨玩家上傳**稀疏**，須先累積足夠 bin（建議 ≥ 數十個時間窗）stage2/3 才可靠；窗太短旋轉判定全是噪音（見 §7-D）。

---

## 6. 與現有研究線 / 前端的關係

- **不動** P7-H H1（experiment/control 事件方向）與 PI 迭代研究（[[p7h-apparatus-broken]] 對應 [人格迭代實驗_規劃_v1.md](人格迭代實驗_規劃_v1.md)）；生態層**消費**它們產出的 9D / outcome，不改其 DV、資料分開存。
- 評分人格來源建議用**冒險終態** `mean_personality`（反映「這場走成什麼」），而非 will 初始向量（§7-C）。
- **V2 收尾接線**（補上你說「目前還不算有」的評分）：冒險崩壞 → 算 score → `POST /ecology/submit` → [CollapseScreen](src/ui/CollapseScreen.gd) 顯示「本場評分 + 你選了稀有/熱門人格 + 當前生態佔比」。見 [[v2-immersive-ui]]。

---

## 7. 待你拍板的子決策

- **A. 岔路**：路 A（靜態平衡）／路 B（旋轉）／A-now-B-later？建議 **A-now-B-later**（cross=0 退化）。
- **B. 原型降維**：3 原型（RPS 對齊，建議）還是 9 職業？投影係數要不要先用既有資料（46 真人 9D）校準？
- **C. 評分來源人格**：冒險終態 `mean_personality`（建議）還是 will 初始向量？
- **D. 時間窗定義**：每 N 筆上傳一個 bin，N=？還是每日？（直接影響 `cycle_metrics` 可靠度）。
- **E. 評分是否影響遊戲性**：稀有加成給**實質獎勵**（誘因 → 閉環生效）還是**純展示**（閉環不啟動，只當資料儀表）？這是「生態會不會真的動」的關鍵。
- **F. 權重動態**：η 與 target —— 路 A 設 `target=1/3`；路 B 不設 target（否則殺掉旋轉）。

---

## 8. 實作改動（最小集合）

- **後端新模組** `api/ecology_tracker.py`：archetype 投影、score 三式（§3）、counts/weights、save/load；+ 3 endpoints（§4）。
- **分析層**：重用 `classify_cycle_level`（**不動**）。
- **前端 V2**：崩壞收尾呼叫 `/ecology/submit` + CollapseScreen 顯示評分（前端在 Windows repo `/mnt/c/Users/n1166/personality-dungeon`）。
- **DB**：新 JSON store，與 p7h 分開。
- 後端改完**手動重啟**（[[backend-restart-required]]）。

---

## 9. 回歸 / 等價檢查

- [ ] 路 A：隨機均勻上傳下，評分趨平、proportions 收斂 ~1/3，`/ecology/assess` 回 **L0**。
- [ ] 路 B：cross>0 + 注入合成循環上傳序列，proportions 出現旋轉，`classify_cycle_level` ≥ **L1**。
- [ ] cross=0 時 路 B 退化 ≡ 路 A（同分數），驗證「A 是 B 退化特例」。
- [ ] `/ecology/assess` 對**合成 L3 序列**回 `level=3`（用 [tests/test_theory_anchors.py](tests/test_theory_anchors.py) 的合成 cycle 當 fixture）。
- [ ] archetype 投影對極端 9D 合理：攻擊型遺言（高 impulsiveness/assertiveness）→ aggressive；與 P7-H 魯莽度 R 同向。
- [ ] 生態 store 與 `p7h_player_test_sessions.json` 完全隔離（互不寫入）。

---

相關：[[p7h-apparatus-broken]] [[v2-immersive-ui]] [[frontend-architecture-doc]] [[backend-restart-required]]
