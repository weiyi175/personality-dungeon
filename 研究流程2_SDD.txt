1️⃣ 增加「控制組定義」（非常重要）

目前你提到：

matrix_ab 且 a=b=0 作為控制組

建議正式寫成 Spec 條文：

Control Baseline Spec

定義：

𝑎
=
0
,
𝑏
=
0
a=0,b=0

則 payoff 為常數矩陣，理論上應收斂。

驗收條件：

cycle_level 必須 = 0

主頻不得達標

振幅不得達標

這叫：

False Positive Guard

沒有這條，你的指標可能只是對噪音敏感。

2️⃣ 加入「參數掃描穩定性條件」

目前驗收是單一參數組。

研究級需要：

在某個參數區間內穩定產生 Level 2 以上。

例如：

a ∈ [0.5, 1.5]
b ∈ [0.5, 1.5]

若只在某一精準點成立，那是數值偶然。

建議加：

Robustness Requirement

在小區間內，cycle_level >= 2 的比例 > 70%

這會大幅提升論文說服力。

3️⃣ 加入「隨機種子穩定性」

目前 seed 只保證重現。

但還需要：

對不同 seed 是否穩定？

建議新增：

同一參數

不同 seed = 10 次

cycle_level 分布

驗收：

Level 2 以上比例 > 某閾值

否則你會遇到：

某些 seed 有循環，某些沒有

這是有限族群模型常見問題。

4️⃣ 加入「振幅標準化」

目前振幅門檻是固定值。

問題：

不同 payoff 尺度會改變比例波動範圍。

建議改為：

𝐴
>
𝜂
⋅
𝜎
𝑛
𝑜
𝑖
𝑠
𝑒
A>η⋅σ
noise
	​


或：

𝐴
>
𝜂
⋅
𝐸
[
𝐴
𝑐
𝑜
𝑛
𝑡
𝑟
𝑜
𝑙
]
A>η⋅E[A
control
	​

]

用 control baseline 的振幅作標準化。

這樣比較科學。

5️⃣ 主頻顯著性建議改為比值而非絕對值

目前：

corr_threshold = 0.3

這在不同長度 T 下意義不同。

更嚴謹方式：

𝑃
(
𝑓
∗
)
mean power
>
𝜅
mean power
P(f
∗
)
	​

>κ

或使用 permutation test。

否則審稿人會質疑門檻任意性。

6️⃣ Phase 方向建議加入「旋轉強度」

目前條件：

大多數 turn_t 符號一致

建議增加：

∣
∑
𝑡
𝑡
𝑢
𝑟
𝑛
𝑡
∣
>
𝛿
∣
t
∑
	​

turn
t
	​

∣>δ

防止：

小幅隨機抖動剛好方向一致。

你已經有寫，但可以明確成公式門檻。

7️⃣ 建議增加「數值漂移防護」

Spec 裡應明確：

所有比例必須重新正規化

避免浮點累積誤差

若 sum 偏離 1 超過 1e-8 → 重新歸一

這是工程級 SDD 必須寫清楚的。

8️⃣ 建議加入「理論對照」

如果你使用 replicator 類更新規則，

可以加：

理論預測：在 a=b 對稱情況下不應產生方向性。

這會讓你的實驗有理論 anchor。

三、真正影響發表成敗的兩件事

如果只能選兩個優化，我建議：

1️⃣ Control baseline 明確寫成驗收條件
2️⃣ 多 seed 穩定性測試

這兩個會決定：

你的循環是否真實，而不是抽樣假象。

四、你目前架構成熟度評估

工程成熟度：8/10
研究成熟度：7/10
發表潛力：高

差的不是架構，而是：

統計嚴謹度

控制組強度

穩定性論證

五、總結

你的 SDD 已經是：

✔ 研究級
✔ 可自動 sweep
✔ 可回歸測試

若再補上：

控制組嚴格驗證

seed 穩定性

robustness 區間

就會變成：

可投稿型演化動態研究框架。

--------------------------------------------------------------------------------------------------

我會先把你這 8 點建議分成「現在值得立刻寫進 Spec + 可落地測試」跟「發表級可再加嚴」兩類；接著我會直接把最關鍵的兩項（Control baseline 驗收 + 多 seed 穩定性）寫進 SDD.md，並把目前 Spec 裡提到但程式尚未支援的 --seed 一併補進模擬器與測試，避免 Spec/實作不一致。

我會把你列的 8 點裡「最有發表價值、且現在就能用最小改動落地」的部分直接寫進 SDD.md，並補一個真正可回歸的 False Positive Guard 測試（control baseline：matrix_ab a=b=0 必須判定為 cycle_level=0）。同時我會把 Stage 3 的「旋轉強度門檻」加進 cycle_metrics.py，避免小幅抖動被誤判成方向一致。

/home/user/personality-dungeon/venv/bin/python -m pytest -q
 Enable shell integration to improve command detection

有修改價值，而且我同意你文末那個優先序：Control baseline（False Positive Guard）跟多 seed 穩定性會最直接影響「你觀測到的循環到底是真結構、還是抽樣假象」。

我已把其中「現在就能落地、且能直接提高嚴謹度」的部分實作進 repo（文件 + 程式 + 測試都補齊）：

Control baseline 條文已寫入 SDD.md（新增 4.4）
明確定義 matrix_ab 且 a=b=0 為控制組
驗收條件寫死：用 w_* 判定時 cycle_level 必須 = 0（振幅/主頻不得達標）
False Positive Guard 已變成可回歸測試：test_control_baseline.py
直接跑一小段 simulation（含 --seed），讀 CSV 的 w_*，用 classify_cycle_level 斷言必為 Level 0
Phase 方向指標依你建議加上「旋轉強度門檻」
cycle_metrics.py 的 phase_direction_consistency 新增 min_turn_strength
測試也補了「超小幅抖動即使方向一致也不能過」：test_cycle_metrics.py
多 seed / 參數鄰域穩健性我先寫入 Spec 作為發表級驗收協議：SDD.md（新增 4.5）
Pr(cycle_level>=2) >= 0.7 的形式已明確化（門檻可調，但先寫死才能避免事後挑門檻）
修正 Spec 內部一致性：6.3 不再寫「應新增 --seed」，因為現在已支援（避免 Spec 自相矛盾）
我也已跑過 pytest -q：23 passed。

你剩下那幾點（振幅標準化、主頻顯著性用 power ratio/permutation test、理論對照 anchor、數值漂移防護）都「很有價值」，但屬於發表級加嚴/統計檢定層，通常會在你先用目前 Level 框架找到穩定區域後，再加上去最划算。

如果你要我下一步接著做「多 seed 穩定性」的自動化（跑 N seeds，輸出 Level 分佈與比例，方便 sweep 報表），我可以用最小方式加一個不破壞分層的 CLI（放在 simulation 端呼叫 analysis 純函數）。你想先做 matrix_ab 的多 seed 報表，還是連 count_cycle 也一起納入比較？

--------------------------------------------------------------------------------------------------


