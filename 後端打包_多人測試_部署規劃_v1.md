# 後端打包 — 多人測試部署規劃 v1（**規劃 only，不實作**）

> 目的：把目前的程式（Godot 前端 + Python 後端）打包發給**多個玩家測試**。本檔只做架構/打包**規劃與取捨**，不寫實作碼。決策要點＝架構選擇**與 β 研究的資料需求綁定**，非純工程題。
> 日期：2026-06-24 ／ 對應 commit 軌見 研發日誌.md 末節。

---

## 1. 目標與限制
- **受眾**：多個**非技術**玩家（朋友），主流 Windows，不會裝 Python/Docker、不會開終端機。
- **要的**：玩家拿到就能玩（低摩擦）；研究要能收到**乾淨、可歸屬、可隔離**的資料（接 β 收集設計）。
- **不要的**：玩家端複雜安裝、巨大下載、防毒一直擋。

## 2. 系統現況盤點（事實，trace 到碼）
| 元件 | 現況 |
|---|---|
| 前端 | Godot 4.6 client，可 export 成 Windows `.exe`。連線位址硬寫 `AppConfig.gd::API_BASE_URL = "http://127.0.0.1:8001"`。場景切換新加 MainMenuScene（本輪）。 |
| 後端 | Python **FastAPI**，`api/server.py` `uvicorn.run(host="0.0.0.0", port=8001)`——**已綁所有介面**（hosted/LAN ready）。 |
| **will→9D** | **server-side** pipeline：遺言 text → **SBERT `BAAI/bge-base-zh-v1.5`**（中文特化、base 級；sentence-transformers + **torch ~2GB**）→ embedding → MLP `outputs/mlp_opus_bgezh_v2.joblib`（Opus 重標，held-out mean r=0.74 vs 舊 qwen mlp_v7 ~0.40）→ 9D。見 `api/personality_sbert_inference.py:27-29`。**（更新 2026-06-25：原 MiniLM+mlp_v7 已換成 bge-zh+v2；模型檔 gitignored、僅本機在。）** |
| 資料持久化 | JSON，`ECOLOGY/PVP/WALLET/P7H_OUT_DIR` 皆 **env 可配**（`server.py:127-139`）。停-改-重啟語意。 |
| ⚠ 依賴清單 | **`requirements.txt` 不完整**：列了 numpy/sklearn/onnx/onnxruntime/pydantic/pytest，**漏掉 fastapi、uvicorn、sentence-transformers、torch**（實際有用）。**打包前必須先重建完整 manifest + pin 版本**（含 torch CPU-only wheel）。 |

## 3. 核心難點：embedding 模型體積
- **torch + bge-zh ≈ 2–2.5GB**＝打包/分發的**主成本**，且只為了 will→9D 這一步。（torch 依賴體積與編碼器無關，MiniLM→bge-zh 主成本結論不變。）
- **瘦身路徑（requirements 已有 onnx/onnxruntime 暗示意圖、但目前 runtime 仍走 torch）**：把 **bge-zh ONNX-export + 量化** → 模型數百 MB（量化後更小，確切體積待量）+ onnxruntime ~50MB，砍掉 torch。**這是讓「每人自帶後端」（B 案）可行的關鍵前置**（屬實作、本檔不做，但列為 gate）。
- 替代：首次啟動才下載模型（縮小安裝包、但首跑要連網 + HuggingFace 可達性）。

## 4. 兩種部署架構 ＋ 研究意涵（**這是主要決策**）

| 面向 | **A. Hosted 中央**（雲端一台後端） | **B. Local per-player**（每人自帶後端） |
|---|---|---|
| 玩家摩擦 | **最低**：只發 Godot `.exe`（數十 MB），零 Python、零模型 | 高：發 game + Python 後端 + 模型（~2GB，或 ONNX 瘦身後 ~200MB）；需 launcher 同起 backend |
| 資料 | **集中、即時、好分析** | 分散在各玩家機器 → **需回收機制**（壓 `reports/` 上傳） |
| **研究隔離（β）** | 共享生態 → **凍結吸引子問題**（見研發日誌 pilot）；需 **server-side per-subject 分庫**（participant_id-keyed ecology store）才乾淨 | **天然 per-subject 隔離**：每人自己 fresh 生態 → 早期稀缺天然高變異 ＝**與 β「per-subject fresh」設計對齊** |
| 體積 | 後端在雲、玩家不下載模型 | 模型體積是痛點（ONNX 為 gate） |
| 安全 | 公開端點 → 需基本保護（token/rate-limit）；Godot HTTPRequest 非瀏覽器、**無 CORS 問題** | 無公開暴露；但 PyInstaller onefile **常被防毒誤報** |
| 成本/維運 | 要 VM + 部署 + 監看（小台即可，N≈友測） | 零雲成本；但版本漂移（玩家跑舊版）難管 |
| 並發 | 多人同時打一台 → 要驗 uvicorn workers + JSON 持久化的序列化（現為 singleton + `asyncio.to_thread` save，**未驗多 worker**） | 單人單後端、無並發 |

## 5. 打包機制細節（依選定架構）

### 共通：Godot 前端 export
- Godot → Export → Windows Desktop `.exe`（+ `.pck`）。
- **`API_BASE_URL` 改 build-time 可配**：抽成 export feature/環境變數或 `config.cfg`，Hosted 指公開 URL、Local 指 `127.0.0.1:8001`。**現在硬寫 localhost 是 A 案的唯一前端阻塞點。**

### A. Hosted 中央（建議小規模友測）
1. 後端依賴重建（§2 manifest）→ 小 VM（2-4GB RAM 夠跑 MiniLM CPU 推論；首載模型較慢）。
2. 反向代理（Caddy/Nginx）+ HTTPS + **基本保護**（簡單 bearer token 或 IP allowlist；防隨機濫用）。
3. **per-subject ecology 分庫**（接 β 設計）：`/ecology/*` 依 `participant_id` 路由到獨立 state（`ECOLOGY_OUT_DIR/<pid>/`），**絕不混入 production 199 筆**。+ `config_version` 標記。← 屬後端 build，本檔只標需求。
4. 驗多並發：先確認 singleton manager + per-submission 序列化在多請求下不打架（或鎖單 worker）。

### B. Local per-player
1. **先 ONNX-export MiniLM**（§3 gate），否則 2GB 不可行。
2. **PyInstaller / Nuitka** onefile 後端 `.exe`（含 onnxruntime + sklearn + fastapi/uvicorn + 模型檔）。
3. **Launcher**：一個 `.bat`/wrapper 先起後端 `.exe`（背景）、等 `:8001` 起來、再開 game `.exe`（或 Godot `OS.execute` 起後端）。
4. `OUT_DIR` 指向玩家本機可寫目錄（`%APPDATA%`），**每人天然獨立**。
5. **資料回收**：結束時打包 `reports/` 提示玩家回傳（或內建一鍵上傳到收集端點）。
6. 防毒誤報：考慮簽章 / 改 onedir（非 onefile）降低誤判。

## 6. 與 β 研究的接點（為什麼這不是純工程題）
- β 識別需要**稀缺跨輪變動 + 獨立受試 + 資料隔離**（見研發日誌 pilot + power 段）。
- **B 案天然滿足 per-subject fresh 生態**（每人自己後端從 fresh 起）→ 直接餵 β 收集；代價是資料回收 + 體積。
- **A 案要在後端補 per-subject 分庫**才達同等乾淨，但玩家摩擦最低、資料最集中。
- 無論哪案：**測試資料必須與 production 199 筆 ecology 完全隔離**（獨立 store + `config_version`），避免污染主線（記取 `isolate-write-targets` 教訓）。

## 7. 建議
- **小規模友測（先驗手感 + 驗「稀缺一動 β 就收窄」）**：**A. Hosted 中央**——玩家零安裝最快上手、資料集中好看趨勢。β 隔離用**後端 participant_id 分庫**達成（中等 build）。
- **若要研究級乾淨 per-subject 隔離且不想維運雲**：**B. Local per-player**，但**先做 ONNX 瘦身**當前置。
- 兩案共同前置（與架構無關，先做）：**(i) 重建完整 `requirements.txt` + pin**、**(ii) `API_BASE_URL` build-time 可配**、**(iii) 測試資料 store 與 production 隔離 + `config_version`**。

## 8. 風險 / 未決
- 防毒誤報（PyInstaller onefile）；公開端點濫用（A 案需 token/rate-limit）；**玩家資料隱私 + 知情同意**（遺言是自由文字，研究用途要告知）；模型授權（MiniLM Apache-2.0，OK）；多 worker 下 JSON 持久化並發未驗；版本漂移（B 案玩家跑舊版）。

## 9. 本檔範圍外（不做）
ONNX 匯出、PyInstaller spec、VM 佈署、per-subject 分庫 build、前端 config 抽取、資料回收端點——**全部待決策架構後另開實作**。

---

## 10. 收集遺言 — 部署 TODO（決策定案 2026-06-25）

> **決策：A. Hosted 中央，host 在開發者自己電腦 + tunnel（非雲 VM）。**
> 主目標 = **擴充真實 will 資料集餵 SBERT 飛輪**；打包只是手段。選自機 host → 後端**零打包**、遺言**直接落本機硬碟**、**零回收成本**。B 案（每人自帶後端 / ONNX / PyInstaller）**不做**。

**已驗證的資料鏈（trace，2026-06-25）：** Godot `/player-test/start` 帶 `will_text`+`is_human=True`（`api/server.py:1518-1525`）→ `/player-test/end` 存 `P7H_OUT_DIR`（`api/server.py:1596`）→ 飛輪 `gather_new_wills()` 遞迴掃 `reports/**` 撿 `will_text`（`scripts/experiments/sbert_flywheel.py:61`）→ Opus 標 → 重訓。實證：P30「減速藥水」筆已被 `--dry-run` 撿到。
**遺言被記是一般遊戲 session 生命週期的一部分（非另一條 gated 研究流程）→「朋友單純玩，遺言就會被記」。**

**🔑 Critical path：** `API_BASE_URL` 可配（FE，P2-1）＋ tunnel 起得來（Ops，P1-2）＝ 朋友能連 ＝ 資料能進。其餘是品質 / 隔離。

### Phase 0 — 收尾現況（快、解鎖其餘）
- [ ] **P0-1** commit live model swap：`api/personality_sbert_inference.py` MiniLM→bge-zh v2（目前已改未 commit，且已是 live 模型）。
- [ ] **P0-2** 修正本 doc §2/§3 過時模型事實（MiniLM→`BAAI/bge-base-zh-v1.5`；torch ~2GB 結論不變、ONNX gate 目標改 bge-zh）。
- [ ] **P0-3** 煙霧測試飛輪：`sbert_flywheel.py --label-only`（需 `ANTHROPIC_API_KEY`）把已排隊 3 筆新 will 入庫，確認標+併流程 OK。

### Phase 1 — 後端可對外（host 自己 PC）
- [ ] **P1-1 [Ops]** 設隔離 store 環境變數：`P7H_OUT_DIR=reports/playtest_2026/`（+ `ECOLOGY/PVP/WALLET_OUT_DIR`）→ 不污染 production 199、仍被飛輪掃到。本機 smoke：玩一局確認 will 落新目錄。
- [ ] **P1-2 [Ops]** 選 + 起 tunnel：**Cloudflare Tunnel（固定免費子域，建議）** 或 ngrok（配 P2-1 的 config 檔）。拿到公開 URL。
- [ ] **P1-3 [BE]** 公開端點基本保護：簡單 bearer token 或短時段 + 不公開 URL（friends-scale 最小化；§4/§7）。
- [ ] **P1-4 [BE, 選]** 重建完整 `requirements.txt` + pin（自機 host 非必須；移 VM 才必要——先記著）。

### Phase 2 — Godot client（唯一要發的成品）
- [ ] **P2-1 [FE]** `API_BASE_URL` 改讀 exe 旁 `config.cfg`（取代 `AppConfig.gd` 硬寫；§5）→ tunnel URL 變了不必重 build。**＝唯一前端阻塞點。**
- [ ] **P2-2 [FE]** `main_scene` = MainMenuScene / 遊戲入口；確認完整迴圈（輸入遺言→玩→死亡→`/player-test/end` 記錄）對**遠端**後端跑通。
- [ ] **P2-3 [FE]** export preset「PlaytestBuild」：排除 dev-only 場景 / p7h 研究 apparatus / screenshot harness；debug overlay gate 在 release。**用排除過濾，不刪原始碼、不複製專案。**
- [ ] **P2-4 [FE]** 遺言輸入：對齊 20 字上限（`_MAX_CHARS`）、送 `is_human=True`、`participant_id` 純收 will 用 `"dev"` 預設即可。
- [ ] **P2-5 [FE]** export Windows `.exe`+`.pck`，**對 tunnel URL 端到端測**：朋友視角玩一局 → 你硬碟出現該 will。

### Phase 3 — 同意 + 發佈
- [ ] **P3-1 [FE]** 一句話告知同意（遺言會被收集訓練遊戲 AI）+ 簡單確認（§8 隱私點）。
- [ ] **P3-2 [Ops]** 打包 `.exe`+`config.cfg` → zip / 雲端連結發朋友 + 簡短玩法說明。

### Phase 4 — 資料收割迴圈（真正的目標）
- [ ] **P4-1** 測試窗口中/後：`sbert_flywheel.py --dry-run` 看新 will → full run 標+重訓（需 API key）。
- [ ] **P4-2** held-out r 有升 → ship v3（repoint `_MLP_PATH` + 重啟，記憶 `backend-restart-required`）。
- [ ] **P4-3** 記 will 數成長 vs held-out r（回答「更多真實資料」槓桿是否還在升）。

### Phase 5 —（延後 / 選）研究級 β
- [ ] **P5-1** 每位朋友獨立 `participant_id`（per-subject β 隔離）；**P5-2** per-subject ecology 分庫。**純收 will 不需要，β 才需要。**

### 風險 / 已知
- 遺言**只在 session END 落地**（`api/server.py:1596`）→ 中途退出可能丟；若良率低，考慮 `/player-test/start` 時也存（BE 小改，掛 P1 可選）。
- 公開端點濫用（P1-3）；**你的 PC 須在線 + tunnel 開著**（自機 host 的代價）。
- 防毒誤報**不適用**（只發 Godot `.exe`，非 PyInstaller）。
- 量級：產量 ≈ 朋友數 × 每人局數 × 每局遺言數；要拉動飛輪可能需十幾人各打幾局。
