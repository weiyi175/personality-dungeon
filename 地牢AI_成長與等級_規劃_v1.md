# 地牢 AI — 成長與等級設計稿 v1

> **STATUS：DRAFT / 討論階段（2026-06-19）。本系統尚未實作（L1 地牢 AI 未建）。**
> 本文件是遊戲願景中「地牢 AI（L1）」這一層的設計收斂稿，純設計、可推翻。
> 上層脈絡見 `人格生態評分_規劃_v1.md`（生態層＝小火龍 concept 2）與遊戲願景記憶。
> 所有公式為 **strawman（打靶用）**，所有 `w_*`/參數 **可調、不寫死**。
> ⚠ **RECONCILE（2026-06-19）**：本稿「成長/等級/防禦收入/clarity」骨幹**存活**；但凡提到「9 桶/簇＝dominance 單位」之處**已被推翻**——faction 改採 **Option 4+**（PC1-PC2 平面 3×120° argmax），9 trait-argmax 桶降為連續 substrate；combat 剋制層＝**authored，待 build+playtest**（H_counter 證偽）。詳見 `地牢counter-policy_L0L1介面_規劃_v1.md` 頂部 banner 與 `地牢設計_待回歸問題_parking_lot.md` A 段。

---

## 0. 定位與邊界

- **層級**：L1（單一玩家的個人 AI 地牢），介於 L0 單局裝置（P7-H）與 L2 小火龍 MetaAI 之間。
- **核心用途**：被別的玩家挑戰（PVP）；它的「強度」要有意義、可成長、可分級。
- **與既有系統的分界**：
  - 餵地牢的是 **Trace（每局新鑄的軌跡資料）**，不是 **Rank（PVP 零和籌碼）**。兩者已分名。
  - 地牢成長可吃 concept 2 的稀有加成，但走**獨立旋鈕**（見 §7），不與分數/金幣綁死。

---

## 1. 核心規則：地牢「剋制主人」

地牢訓練的目標 = **剋制它的主人玩過的 playstyle 分佈**（決策已定）。這條規則免費長出兩個 emergent 後果：

1. **自帶反同質化**：若全體玩家收斂到同一流派 X，則人人地牢都剋 X → 玩 X 的人去打別人地牢都被硬剋 → 逼玩家分化。方向與 concept 2 一致。
2. **「偵察 + 剋制」PVP 深度**：挑戰者可觀察一座地牢的訓練偏食（主人都玩攻擊→地牢只會防攻擊），帶一個它沒學過的 build 去鑽漏洞。

> ⚠ 已知岔路：地牢剋「主人」而非「當前挑戰者」，是刻意選擇（換到上述兩個好處），但代表**主人養強地牢的受益者不是主人自己** → 「主人養強地牢能換到什麼」**已補完，見 §11 防禦收入迴路**。

---

## 2. 兩層模型：成長（訓練）／ 等級（訓練品質）

| 層 | 是什麼 | 類比 |
|---|---|---|
| **成長 ΔL** | 每局 Trace 餵進去後地牢能力的增量 | 一次訓練 step |
| **等級 Level** | 累積訓練後的「訓練品質」分級 | 模型最終 benchmark |

**精確化（ML 兩條軸，勿混）**：
- **資料軸（輸入：你餵了什麼）**：樣本數量、品質、多樣性。
- **模型軸（輸出：它多強）**：對真實挑戰者的勝率＝唯一 ground truth。
- **學習率＝旋鈕（超參數），不是成績**；它是 §8 的道具/風險機制，不計入等級。

---

## 3. 關鍵原理：多樣性 = 泛化 = 可破解性

地牢學的是主人的 playstyle 分佈，所以：

- **低多樣性地牢（單食）**：對那一招超強，但換流派的挑戰者來打就送頭 ＝ **過擬合**。
- **高多樣性地牢**：能剋一大片 playstyle ＝ **泛化**，對所有挑戰者都難打。

⇒ **「樣本多樣性」不是裝飾加分，它就是「泛化／抗破解」本身。** 這讓多樣性指標有真實意義，也是 §4 成長公式裡多樣性走**相乘閘門**（而非相加）的理由。

---

## 4. 成長公式（strawman）+ 權重表

成長拆三類來源：**A 逐局相加 × B 投資組合閘門 × C 定向/投資**。

### A. 逐局貢獻（每條 Trace 加多少，相加）
| 因子 | 符號 | 權重 | 說明 |
|---|---|---|---|
| 基礎 | — | `w_base` | 玩一局就有的底 |
| 評分 | `score_i` | `w_score` | 高分→長更快（score 含 concept2 稀有加成） |
| 稀有 type | `is_rare_i` | `w_rare` | 玩當前稀缺 type **獨立加權**（見 §7） |
| 樣本品質 | `clarity_i` | `w_quality` | 遺言人格訊號清晰度（噪聲遺言貢獻低）— **定義待拍板 §8.1** |
| 存活長度 | `surv_i` | `w_survival` | 軌跡越長資料點越多 |

### B. 投資組合閘門（整批 Trace 的性質，相乘）
| 因子 | 符號 | 權重 | 說明 |
|---|---|---|---|
| 覆蓋率/多樣性 | `C` = 9 桶熵 | `w_cover` | **泛化閘門**：低多樣性 → 容量打折（過擬合懲罰） |
| 總量 | `V = log(1+n)` | — | 邊際遞減 |
| 新鮮度衰減 | `decay(i)` | `λ_decay` | 舊 Trace 緩慢失權 → 地牢持續適應、不被陳舊招式釘死 |

### C. 定向 / 投資
| 因子 | 符號 | 權重 | 說明 |
|---|---|---|---|
| 定向覆蓋 | `directed_i` | `w_directed` | 覆蓋「**打贏過你**」的 type（hard-negative）→ 比盲目覆蓋值錢（§6） |
| 學習率道具 | `η` | — | 花金幣加速成長，**附過擬合風險**（§8.3） |

### 合併
```
逐局貢獻 g_i = w_base + w_score·score_i + w_rare·is_rare_i
             + w_quality·clarity_i + w_survival·surv_i + w_directed·directed_i

ΔL = η · [ Σ_i decay(i)·g_i ] · (0.5 + 0.5·C) · scale(V)
            └ A 相加 ┘           └ B 多樣性閘門 ┘
```
`(0.5 + 0.5·C)`：多樣性低 → 容量天花板被壓到一半 ＝ 過擬合的數值懲罰，把「單食地牢易被破解」寫進等級。

---

## 5. 等級 = 訓練品質（strawman + 真實校準）

```
Capacity        = ΔL 累積（§4 的輸出）         # 由前四因子算出的「預估強度」
ActualStrength  = Capacity 經真實挑戰勝率校準    # ground truth 修正
DungeonLevel    = 分箱(ActualStrength)
```
**紀律**：量/質/多樣/學習率都只是**預測因子**；**最終表現（真實 PVP 勝率）是唯一 ground truth**。等級必須**定期用真實勝率校準**，否則「Lv.10 打不贏人」＝假數字。

---

## 6. Hard-negative mining 閉環（攻打雷達）

機制：**顯示攻擊者攻打時用的 9D 性格雷達** → 主人研究 → 寫遺言靠近那一類 → 地牢學會防那一類。＝ ML 的 hard-negative mining。

```
攻擊者找到你的盲區 → 你在攻打紀錄看到他用什麼性格
→ 你寫遺言覆蓋那盲區 → 地牢學會防 → 攻擊者再找新盲區 …
```
連續、emergent、自我平衡；逼玩家覆蓋整個 type 空間 → 全域多樣性 → 餵 concept 1 滿覆蓋。

**兩個防鑽紀律**：
1. **只從「打贏你的攻擊」學**（真 hard-negative）；被你防下的攻擊不含資訊、不餵訓練。
2. 此規則**自動擋投毒**：誤導性格攻擊若**失敗**學不到東西；若**成功**那本就是你該補的真漏洞。
3. **定向覆蓋 > 盲目覆蓋**（§4-C 的 `w_directed`）。

---

## 7. 三條獨立旋鈕（稀有度解耦）

稀有 type 同時影響三件事，**三條旋鈕各自可調、互不牽動**：

| 旋鈕 | 影響 | 位置 |
|---|---|---|
| 稀有 → **金幣** | 玩家經濟誘因 | 生態層 score_to_coins |
| 稀有 → **分數** | concept 2 計分 | 生態層 |
| 稀有 → **地牢成長** | `w_rare` | 本文件 §4-A |

理由：`score_i` 已把稀有加成耦合進分數；再給獨立 `w_rare`，讓「稀有對**地牢成長**的影響」可單獨調，不動金幣/分數平衡。

---

## 8. 待拍板項（TBD）

### 8.1 樣本品質 `clarity_i` — ✅ 鎖定（暫定 B：桶信心主導）
目的＝衡量「這條 Trace 的人格訊號有多清楚」（清楚＝乾淨訓練標籤；模稜＝噪聲）。**只看遺言向量**（自動遊玩下軌跡是遺言+seed 的決定性函數，無獨立訊號）。
```
p          = softmax(|trait_scores| / τ)        # τ 調峰銳度（可調）
clarity_b  = 1 − H(p)/log(9)                    # 桶信心 ∈ [0,1]，主項
extremity  = min(1, ‖vec−baseline‖ / ε_ref)     # 強度，capped
clarity_i  = clarity_b · (0.7 + 0.3·extremity)  # margin 主導，extremity 只給 ±30%
```
三個設計選擇：① **margin 主導**＝品質核心是「標籤清不清楚」非「人格多極端」；② **extremity 只 0.3 且 capped**＝保留強訊號略加分，但不餿死 balanced、不誘導全民極端；③ **絕對值非 population z-score**＝clarity 是樣本內在性質，不隨母體浮動。
- 否決候選 A（純 extremity）：偏袒極端、**惡化 balanced 餓死**、Goodhart。
- 候選 C（SBERT→MLP 模型信心）：最 principled 但需模型暴露 per-trait variance；**留作 v2 升級**（待查 MLP 是否支援）。
- Goodhart 檢查：刷 clarity＝寫「清楚的某一型」遺言＝真乾淨樣本，非作弊；只要不用純 extremity 就免疫。

### 8.2 攻擊方是否可「偵察」地牢訓練偏食 —（暫定：不對稱資訊）
- **不對稱資訊（暫定採用）**：只有防守方事後看攻擊者性格（§6 hard-neg）；攻擊方**盲打**，靠多打幾次自己摸索偏食。守方資訊優勢、保留神秘感、攻方有探索成本。
- 全資訊（攻方可事先偵察挑剋制 build）：counter-pick 博弈更深但降神秘感 → **留作 v2 可選開關**。
- ⚠ 暫定理由：先讓「偵察」是**靠遊玩試出來的隱性技巧**而非 UI 給的明牌；若上線後攻方體感太盲再放寬。

### 8.3 學習率道具的過擬合風險曲線 —（暫定：凸函數、有甜區）
- 高 `η` ＝ 成長快，但**偏食加劇**（過度貼近最近 Trace）→ 更易被鑽。
- 設計成 **risk/reward**：`η` 越高 → §4-B 的有效 `C` 衰減越快（近期 Trace 權重過度集中）。
- **暫定曲線＝凸 + 甜區**：`η ∈ [1, η_sweet]` 淨正報酬（成長快、偏食可控）；`η > η_sweet` 後過擬合懲罰超過成長收益、淨負。讓道具有「適量加速」的最佳買點，不是越多越好。`η_sweet` 與懲罰斜率 **待數值校準**。

---

## 9. 與其他系統的接點

- **L2 小火龍 concept 2 ＝ 生態層**（稀有計分），透過 `w_rare`/score 接入；走**路線 B**（concept 2 只動玩家面分數/金幣，不對 concept 1 訓練資料動刀）。
- **粒度 ＝ 9 桶**（trait argmax），覆蓋率 `C`、`directed`、`clarity` 全建在 9 桶上（不再追 RPS 旋轉）。
- **Trace（餵地牢/小火龍，每局新鑄）vs Rank（PVP 零和）** 分名。
- **★ L0↔L1 介面（地牢 counter-policy 怎麼從 Trace 學）＝整套地基**，獨立規劃於 `地牢counter-policy_L0L1介面_規劃_v1.md`。本稿的 π_B、`w_rare`、多樣性閘門全部依賴它的「命門假設」（不同 playstyle 弱點方向不同）成立——**建前須先實測驗證**。

---

## 10. 冷啟動

- **資料不足門檻前**（例：主人 < 10 條命 Trace）：用**規則式/腳本地牢**或**全體平均地牢** bootstrap。
- 累積夠了再交棒給學出來的 AI。每個 AI 層都要明定 **N 門檻 + fallback**，沒料時不裝死。
- 好消息：小火龍 concept 1 不是真冷啟動——現有 26 真人 + 296 sim + 迭代 Trace 已是起始語料。

---

## 11. PVP 結算與主人誘因迴路（補完 §1 開放問題）

**結算機制**：A 挑戰 B 的地牢 ＝ A 玩一條命穿過 B 地牢生成的事件場景（該場景**剋制 B 主人的 playstyle**）。
- **攻方勝**：A 存活 / 達標（撐過剋制事件）。
- **守方勝**：A 崩壞（地牢防住）。
- 因為地牢剋的是「B 主人」，**A 若 playstyle 與 B 不同 → 地牢調錯方向 → A 較易過**（兌現 §1 的「偵察偏食→帶剋制 build」）。

**主人誘因 ＝ 防禦收入（核心迴路）**：
```
B 養強地牢 → 攻擊者更常失敗 → 失敗者掉 Rank、B 被動收 Rank
          → B 的地牢投資透過「防禦收入」回本
```
這補上 §1：主人不打自己地牢，但**強地牢＝被動 Rank 印鈔機**。投資地牢（多玩、覆蓋盲區、buy 學習率）→ 防禦勝率↑ → 防禦收入↑。

**賭注隨等級縮放**：打贏高 `DungeonLevel` 地牢 → 拿更多 Rank；攻高等地牢失敗 → 掉更多。風險/報酬隨等級走，高等地牢是高風險高報酬的挑戰目標。

---

## 12. 每場 PVP 的資料與經濟流

一場「A 攻 B 地牢」同時產生四條流：

| 流 | A 勝（攻方過關） | B 勝（地牢防住） |
|---|---|---|
| **Trace**（餵小火龍/各自地牢） | A 這條命的軌跡 → A 自己的地牢 + concept 1 | 同左（A 仍玩了一條命） |
| **hard-negative**（§6） | A 的 9D 攻擊性格**入 B 訓練庫**（真打贏＝真漏洞） | 不入庫（被防下、不含資訊） |
| **Rank**（零和，§3.4 參數化） | B → A 轉 `rank_stake` | A → B 轉 `rank_stake`（B 防禦收入） |
| **金幣** | — | A 敗可花金幣抵銷 ≤ `offset_ratio`(50%) Rank 損失 |

> 注意零和守恆（§3.4）：金幣抵銷採**守恆版**——A 拿到的＝B 實掉的，金幣只讓敗方少掉分，不憑空鑄 Rank。

---

## 13. 反 farm 與配對

- **farm 風險**：專挑弱地牢刷攻擊收入、或掛超弱地牢誘敵刷防禦收入。
- **緩解（暫定）**：
  1. **Rank 分段配對名單**（§3.2 自選名單按段位過濾）。
  2. **重複攻打同目標遞減**（短時間內反覆打同一地牢，Rank 報酬遞減）。
  3. **攻擊門票成本**（小額 Rank/金幣，擋無腦掃榜）。
  4. **「剋主人」自我緩解**：同流派目標天生難打，farm 同類地牢不划算。

---

## 14. 一句話

地牢成長 = **A 逐局相加（含獨立 `w_rare`）× B 多樣性閘門 × C 定向/投資**；等級 = 訓練品質（真實 PVP 勝率校準）。軸心原理：**多樣性 = 泛化 = 可破解性**。主人誘因 = **防禦收入迴路**（強地牢＝被動 Rank 印鈔，補完「養強圖什麼」）。攻打雷達 = hard-negative mining，配「只從打贏你的攻擊學」同時防作弊＋防投毒。三條稀有旋鈕解耦。clarity ✅ 鎖桶信心；攻方偵察暫定不對稱；學習率暫定凸+甜區。
